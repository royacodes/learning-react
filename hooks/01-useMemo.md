# ا🔵 useMemo چیه؟

ا**useMemo** یکی از هوک‌های React هست که برای بهینه‌سازی عملکرد (Performance Optimization) استفاده می‌شود.

به زبان خیلی ساده:

ا✔️ useMemo نتیجه‌ی یک محاسبه سنگین را یادداشت می‌کند

تا لازم نباشد هر بار کامپوننت رندر می‌شود دوباره محاسبه انجام شود.

# 🟦 چرا به useMemo نیاز داریم؟

در React هر بار state یا props تغییر کند → کامپوننت دوباره رندر می‌شود.
این یعنی تمام فانکشن‌های داخل رندر دوباره اجرا می‌شوند.

اگر یکی از این فانکشن‌ها:

محاسبه سنگین دارد

روی آرایه بزرگ loop دارد

اsorting سنگین انجام می‌دهد

داده‌ها را فیلتر می‌کند

یا عملیات CPU-heavy انجام می‌دهد

پس به ازای هر رندر، هزینه سنگین محاسبه تکرار می‌شود.

❗ اینجا useMemo وارد می‌شود:

می‌گوید:

«اگر ورودی‌ها تغییری نکرده‌اند، نتیجه قبلی را بده، دوباره محاسبه نکن.»

# 🟦 مثال ساده از useMemo

بدون useMemo (هر بار رندر → محاسبه تکرار می‌شود)


```jsx
const result = heavyCalculation(data); // خیلی سنگین

```



با useMemo (فقط وقتی data تغییر کند)

```jsx
const result = useMemo(() => {
  return heavyCalculation(data);
}, [data]);

```

◀ حالا تا وقتی data تغییر نکرده باشد React نتیجه قبلی را کش می‌کند و فانکشن heavyCalculation دوباره اجرا نمی‌شود.

# 🟦 یک مثال واقعی: محاسبه سنگین
```jsx
const expensiveValue = useMemo(() => {
  let total = 0;
  for (let i = 0; i < 500000000; i++) {
    total += i;
  }
  return total;
}, []);
```
اگر useMemo استفاده نکنیم، این حلقه سنگین در هر رندر از صفر اجرا می‌شود و UI به شدت کند می‌شود.

# 🟦 دو کاربرد اصلی useMemo
## ✔️ 1. جلوگیری از محاسبات سنگین غیرضروری
مثل:

اsort

اfilter

اreduce

اloops روی داده بزرگ

پردازش CPU سنگین
## 2. ثابت نگه‌داشتن reference برای جلوگیری از رندرهای اضافی childها
برای مثال:
```jsx
const options = useMemo(() => ({ theme: "dark" }), []);
```

اگر این کار را نکنیم:
هر بار کامپوننت والد رندر شود → یک object جدید ساخته می‌شود
و کامپوننت فرزند دوباره رندر می‌شود (حتی اگر props تغییر نکرده باشد)
# 🟦 چه زمانی نباید از useMemo استفاده کنیم؟
برای چیزهای ساده (مثل یک جمع یا یک string کوچک)

برای فانکشن‌های سبک

وقتی از آن استفاده شود ولی سود performance ندهد → خودش overhead ایجاد می‌کند

# 🟦 خلاصه (Cheat Sheet) — useMemo

| سوال | جواب |
|------|--------|
| **useMemo چیه؟** | ابزاری برای *ذخیره (Cache)* نتیجه یک محاسبه |
| **کی اجرا می‌شود؟** | فقط وقتی مقدارهای داخل `dependency array` تغییر کنند |
| **برای چی خوبه؟** | جلوگیری از انجام دوباره محاسبات سنگین در هر رندر |
| **آیا همیشه لازم است؟** | نه — فقط در محاسبات سنگین یا زمانی که reference باید ثابت بماند |
| **مشابه چی در جاوااسکریپت است؟** | Memoization (ذخیره نتیجه تابع برای استفاده دوباره) |











