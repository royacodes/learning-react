# 🔵 هوک (Hook) در React چیست؟

ا**Hook** ها توی React تابع‌هایی هستند که اجازه می‌دهند در کامپوننت‌های تابعی از قابلیت‌های داخلی React مثل **state، lifecycle و context** استفاده کنیم.

به زبان ساده:

ا> **Hook = ابزاری برای اضافه‌کردن رفتار React به کامپوننت‌های تابعی.**

قبل از معرفی هوک‌ها، برای داشتن state مجبور بودیم از **کلاس‌ها** استفاده کنیم.  
اما با آمدن هوک‌ها، کامپوننت‌های تابعی قدرتمند و کامل شدند.

---

## 🟦 چرا اسمش "Hook" است؟

چون این تابع‌ها مثل «قلاب» به کامپوننت شما وصل می‌شوند و قابلیت جدیدی اضافه می‌کنند:

- «قلاب کن به React تا state داشته باشم»
- «قلاب کن تا بعد از رندر یک کار انجام بدم»
- «قلاب کن تا نتیجه یک محاسبه رو ذخیره کنم»

---

## 🟦 معروف‌ترین هوک‌های React

| هوک | کاربرد |
|------|--------|
| **useState** | مدیریت state |
| **useEffect** | اجرای عملیات بعد از رندر (side effects) |
| **useMemo** | جلوگیری از محاسبات تکراری و سنگین |
| **useCallback** | جلوگیری از ساخت دوباره توابع در هر رندر |
| **useRef** | نگه‌داری مقدار بدون ایجاد رندر |
| **useContext** | استفاده از context بدون prop drilling |

---

## 🟦 مثال ساده از استفاده هوک

```jsx
function Counter() {
  const [count, setCount] = useState(0);

  return (
    <button onClick={() => setCount(count + 1)}>
      Clicked: {count}
    </button>
  );
}
```

بدون هوک useState این کامپوننت کاملاً ثابت و بدون تعامل می‌شد.

## 🟦 هدف اصلی هوک‌ها
ساده‌کردن مدیریت state

ساده‌کردن lifecycle

حذف کلاس‌ها و ساده‌تر شدن کدنویسی

تولید منطق قابل‌استفاده‌مجدد (Custom Hooks)

بهینه‌سازی performance (مثل useMemo, useCallback)


## 🟦 تعریف یک‌خطی

هوک‌ها توابعی هستند که قابلیت‌های React را به کامپوننت‌های تابعی اضافه می‌کنند.

