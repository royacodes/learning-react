# ا🔵 useCallback چیست؟

اuseCallback کاری شبیه useMemo انجام می‌ده،
ولی به‌جای ذخیره نتیجه یک محاسبه،
خودِ تابع (function) رو ذخیره می‌کنه تا در هر رندر دوباره ساخته نشه.

به زبان خیلی ساده:

اuseCallback = جلوگیری از ساخت دوباره تابع در هر رندر

## 🔥 چرا اصلاً باید جلوی ساخت دوباره تابع را بگیریم؟

چون در React هر بار کامپوننت رندر شود → همه توابع دوباره ساخته می‌شوند.

این مشکل زمانی خطرناک می‌شود وقتی:

✔ تابع را به فرزند (child component) پاس بدهیم

اگر فرزند با React.memo بهینه شده باشد،
اما ریفرنس تابع هر بار عوض شود → فرزند دوباره رندر می‌شود!

و این کل بهینه‌سازی را بی‌اثر می‌کند.
# ا🟦 useCallback دقیقاً چه کار می‌کند؟

می‌گوید:

«اگر مقدارهای dependency تغییر نکرده‌اند،
همان نسخه قبلی تابع را بده.
تابع جدید نساز.»



## 🟦 مثال ساده:
بدون useCallback (تابع هر بار رندر دوباره ساخته می‌شود)
```jsx
const handleClick = () => {
  console.log("clicked");
};

```
با useCallback (تابع فقط وقتی dependencies تغییر کنند ساخته می‌شود)
```jsx
const handleClick = useCallback(() => {
  console.log("clicked");
}, []);

```
# 🟦 مثال واقعی (مهم!)
کامپوننت فرزند با React.memo:


```jsx
const Child = React.memo(({ onAdd }) => {
  console.log("child rendered!");
  return <button onClick={onAdd}>Add</button>;
});
```

کامپوننت والد:

❌ بدون useCallback → فرزند هر بار رندر می‌شود:
```jsx
function Parent() {
  const [count, setCount] = useState(0);

  const add = () => setCount(count + 1);

  return <Child onAdd={add} />;
}
```
✔ با useCallback → فرزند فقط وقتی لازم باشد رندر می‌شود:

```jsx
function Parent() {
  const [count, setCount] = useState(0);

  const add = useCallback(() => {
    setCount(c => c + 1);
  }, []);

  return <Child onAdd={add} />;
}
```
# 🟦 چه زمانی از useCallback استفاده کنیم؟
✔ زمانی که تابع را به child می‌فرستی

و child با React.memo بهینه شده است.

✔ زمانی که تابع سنگین نیست اما “reference stability” مهم است

(تابع نباید هر بار جدید شود)

✔ زمانی که dependency ها به ندرت تغییر می‌کنند
# 🟦 چه زمانی استفاده نکنیم؟

❌ برای توابع معمولی داخل همان کامپوننت

❌ وقتی قرار نیست تابع به child پاس داده شود

❌ زمانی که فکر می‌کنیم performance بهتر می‌شود ولی عملاً overhead اضافه می‌شود


















